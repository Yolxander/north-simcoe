[build]
command = "npm ci && npm run build"
publish = "dist"

[build.environment]
NODE_ENV = "development"
NPM_FLAGS = "--legacy-peer-deps"
NODE_VERSION = "20"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
